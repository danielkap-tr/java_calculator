apiVersion: v1
kind: Service
metadata:
  name: {{ include "calculator-api.fullname" . }}
  labels:
    {{- include "calculator-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "calculator-api.selectorLabels" . | nindent 4 }}
